# 기여 / 작업 규칙

개인 저장소지만, 몇 달 뒤의 나도 남이다. 규칙을 적어둔다.

## 스킬 추가

직접 폴더를 만들지 말고 `/new-skill` 을 쓴다. 그게 이 저장소의 존재 이유다.

수동으로 만들어야 한다면 최소한 이건 지킨다.

- [ ] `skills/{이름}/SKILL.md` 위치
- [ ] `name` 이 폴더 이름과 같음
- [ ] `description` 에 실제 발화 표현이 3개 이상 들어감
- [ ] 본문 500줄 이내 (넘으면 `references/` 로 분리)
- [ ] 트리거 테스트 완료 — 걸려야 할 문장 3개 / 안 걸려야 할 문장 2개
- [ ] `README.md` 스킬 목록 표에 한 줄 추가
- [ ] `CHANGELOG.md` 에 기록

## 스킬 수정

증상에 따라 고치는 곳이 다르다. 헷갈리면 여기를 본다.

| 증상 | 고칠 곳 |
|---|---|
| 안 불려온다 / 아무 때나 불려온다 | `description` |
| 불려오는데 결과가 별로다 | `SKILL.md` 본문 절차 |
| 본문이 500줄을 넘었다 | `references/` 로 분리 |
| 같은 코드를 반복 생성한다 | `scripts/` 로 추출 |

**한 번에 하나만 바꾸고 바로 검증한다.** 두 개를 같이 바꾸면 뭐가 효과였는지 모른다.

## 커밋 메시지

```
feat(skill): write-post 추가
fix(new-skill): description 에 반말 표현 추가
docs: quickstart 링크 경로 수정
refactor(new-skill): 상세 규칙을 references 로 분리
```

스코프에는 스킬 이름을 쓴다. 나중에 특정 스킬의 이력만 뽑아볼 때 편하다.

## 버전

`plugin.json` · `marketplace.json` · `CHANGELOG.md` 세 곳의 버전을 함께 올린다.

- 스킬 추가 → minor
- 기존 스킬 수정 → patch
- 구조 변경 (설치 방식, 경로) → major

## 하지 말 것

- 검증 안 한 스킬 커밋
- 한 스킬에 여러 절차 욱여넣기
- 프로젝트별 컨벤션을 스킬에 하드코딩 (그건 그 프로젝트 `CLAUDE.md` 로)
- 안 쓰는 스킬 방치 — 3개월간 안 썼으면 지운다. 지워도 git 에 남는다.
