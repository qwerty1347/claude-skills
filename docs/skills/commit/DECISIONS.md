# /commit

## 무엇을 하는가

변경 내용을 읽고 Conventional Commits 형식으로 커밋하는 하네스. **push 는 하지 않는다.**

## 왜 만들었는가

`git status` 만 보고 메시지를 지으면 `update files` 같은 커밋이 쌓인다.
매번 "diff 를 보고 왜 고쳤는지 쓰라" 고 지시하던 것을 굳혔다.

## 어떻게 쓰는가

`/commit` 으로만 부른다. 자연어로는 불려오지 않는다.